# 测试报告

初版日期：2026-09-09；3D 主题更新：2026-09-10。

## 2.0 更新验证

- 核心测试共 12 项通过。八主题、三难度、各 100 个种子，共 2,400 组关卡；另验证八主题各难度可完整通关。
- 原有 16 项浏览器流程再次通过，包括 108 件物品通关、手机触摸、刷新存档、失败恢复及四种视口布局。
- 八主题分别验证 108 件物品的非空画面、模型缩略图、提示和三消；主渲染的绘制调用数为 49–58。
- 原有果园存档及寄存物品可以继续；手动省电模式保留进度；模拟 WebGL2 不可用时自动回退到烘焙图片模式。
- 参考视频仅用于观察设计，没有提取视频画面作为游戏素材，也没有上传视频或其他本机资料。

## 连点对比

同机 Chrome、390 × 844 视口、4 倍 CPU 降速，分别执行 36 次指针选取，并在每组三次之前使用提示。统计的是同步点击处理耗时，不是整机帧率。

| 指标 | 更新前 | 更新后 |
| --- | ---: | ---: |
| 点击处理耗时中位数 | 10.9 ms | 2.5 ms |
| 点击处理耗时 95 分位 | 13.5 ms | 5.1 ms |
| 测试期间存档写入次数 | 147 | 1 |
| 测试期间新增图片节点数 | 684 | 24 |
| 最终消除物品数 | 36 | 36 |

测试观察到的主线程长任务数均为 0。此次优化减少了点击处理的工作量，不能据此承诺所有真实手机都能达到固定帧率；尚未在朋友的实际设备上复测。

存档改为交互停止 300 毫秒后合并写入，离开页面或切到后台时立即保存。计时显示按秒变化，空闲时不重复绘制 3D 场景。

## 环境

- Windows 本地运行。
- Node.js 24.19.0。
- Playwright 驱动本机 Chrome，使用独立的测试浏览器上下文。
- 桌面视口：1440 × 1080、1440 × 900、768 × 1024。
- 手机视口：390 × 844、320 × 740；另含真实触摸事件模拟。

## 核心规则：10 项通过

1. 三场景、三档难度、100 个种子，共 900 组关卡的完整三件组与数量校验。
2. 相同种子可复现，不同种子打乱顺序不同。
3. 三消与撤回恢复。
4. 第七格完成三消时先消除，不误判失败。
5. 七格不匹配时失败，撤回后可继续。
6. 三格寄存容量、撤回寄存、取回和三消。
7. 三档难度可结算获胜，不能通过撤回获胜重复刷纪录。
8. 提示优先已有两件的种类。
9. 异常存档和异常撤回历史被拒绝。
10. 5,000 次随机操作后，数量、容量和存档仍保持一致。

## 浏览器流程

- 原创素材全部载入，Canvas 非空并包含多种有效像素。
- 使用实际鼠标坐标点击，不直接调用规则函数代替 UI 测试。
- 选取、寄存、取回、撤回、三消。
- 刷新恢复当前局。
- 颠锅后物品实际发生位移。
- 设置弹窗暂停计时，音效按钮可切换。
- 切换场景时的确认与取消；难度切换到 108 件物品。
- 保存失败状态后的恢复弹窗与免费救回。
- 获胜结算、鹅友解锁、再开新局。
- 使用不限次提示与实际鼠标点击，完成 108 件物品挑战局，36 组三消后正常获胜。
- 键盘可访问的物品列表。
- 四种视口无水平溢出，主要控件文字不溢出。
- 移动模式下实际触摸事件可选取物品。
- 浏览器控制台无错误。
- 单文件离线 HTML 通过 file 协议打开，36 件物品、物理引擎、图标、提示均可用；没有外部脚本或样式依赖，也无页面异常。

## 公网发布验证

- 已发布至 https://xiaopengy240.github.io/catch-a-goose/ 。
- GitHub Actions 发布成功，Pages API 确认强制 HTTPS。
- 16 项浏览器检查已在实际公网网址重新执行并通过，包括 108 件物品整局通关和手机触摸模拟。
- 网页由 GitHub Pages 静态托管，不需要本机预览服务或临时隧道保持在线。

## 本轮修复

- 首次颠锅在页面加载后 450 毫秒内会被错误拦截，已修复。
- 提示使用独立于正常堆叠的最上层，避免高难度下被普通物品覆盖。
- Windows 保留目录名导致的开发启动问题已绕开；最终另提供可移到普通目录直接运行的单文件版。

## 未验证范围

- 未使用真实 iOS / Android 设备或实际微信内置浏览器。
- 未进行微信小程序 / 小游戏发布和审核。
- 未承诺兼容所有旧版浏览器；本轮测试不能替代上线后的真实设备回归。
